using UnityEngine;
using System.Collections;

public class RightHandSword : MonoBehaviour {
    public GameObject rightHandController;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //transform.position = rightHandController.transform.position;
        ////transform.rotation = rightHandController.transform.rotation;
        //Vector3 angles = rightHandController.transform.rotation.eulerAngles;
        //transform.rotation = Quaternion.Euler(90-angles.x, angles.y, angles.z);
	}
}
